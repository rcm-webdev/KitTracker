{
  "name": "kittracker",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --workspace=@kittracker/server",
    "dev:client": "npm run dev --workspace=@kittracker/client",
    "test:e2e": "npm run test --workspace=@kittracker/e2e",
    "db:migrate": "npm run db:migrate --workspace=@kittracker/server",
    "db:seed": "npm run db:seed --workspace=@kittracker/server"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "shadcn": "^4.2.0"
  }
}
